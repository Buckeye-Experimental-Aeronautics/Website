# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling, never part of the deployed static site
.claude/
node_modules/
*.log

# The old design-staging working copy (its own nested git repo; not part of
# this repo's history) — kept on disk locally for reference, never tracked.
WebsiteRefresh/
